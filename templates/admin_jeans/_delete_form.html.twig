<form method="post" action="{{ path('admin_jeans_delete', {'id': jeans.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jeans.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
