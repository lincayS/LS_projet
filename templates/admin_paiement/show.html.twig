{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ paiement.createdAt ? paiement.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>StripeSessionId</th>
                <td>{{ paiement.stripeSessionId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_paiement_index') }}">back to list</a>

    <a href="{{ path('admin_paiement_edit', {'id': paiement.id}) }}">edit</a>

    {{ include('admin_paiement/_delete_form.html.twig') }}
{% endblock %}
