{% extends 'base.html.twig' %}

{% block title %}Purchase{% endblock %}

{% block body %}
    <h1>Purchase</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ purchase.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ purchase.createdAt ? purchase.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ purchase.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_purchase_index') }}">back to list</a>

    <a href="{{ path('admin_purchase_edit', {'id': purchase.id}) }}">edit</a>

    {{ include('admin_purchase/_delete_form.html.twig') }}
{% endblock %}
