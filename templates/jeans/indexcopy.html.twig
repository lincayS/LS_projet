{% extends 'base.html.twig' %}

{% block title %}Jeans index{% endblock %}

{% block stylesheets %}
 <link rel="stylesheet" href=" {{ asset('css/stylecat.css')}}">

{% endblock %}

{% block body %}
    <h1>Jeans index</h1>

    
        {% set myVal = - 1 %}

        {% for jeans in jeans %}
     {% set myVal = myVal + 1 %}
     {{ myVal }}
{% if myVal is divisible by(4) %}
{{'<div class="all" >'}}
{% endif %}
<div class="liens">
            <a href="{{ path('jeans_show', {'id': jeans.id}) }}">
            <img src=" {{ asset('img/ {{ jeans.picture }}')}}" alt="">
            <p> {{ jeans.name }}</p>
            </a>
       
        
    
      <form action="/cart/add" method="POST">
        <input type="hidden" value="{{jeans.id }}" name="Jeans-cart-id" /> 
        <input type="hidden" value="{{jeans.id }}" name="header" />

        <button  id="btn" type="submit" class="btn btn-dark mt-3"><i class="fas fa-cart-plus"></i></button>
      </form>
 </div>
{% if (myVal + 2) is divisible by(4) %}
{{'</div>'}}
{% endif %}
            
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
       

    <a href="{{ path('jeans_new') }}">Create new</a>
{% endblock %}
