{% extends 'base.html.twig' %}

{% block title %}Jeans{% endblock %}

{% block body %}
    <h1>Jeans</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jeans.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ jeans.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ jeans.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ jeans.price }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ jeans.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('jeans_index') }}">back to list</a>

    <a href="{{ path('jeans_edit', {'id': jeans.id}) }}">edit</a>

    {{ include('jeans/_delete_form.html.twig') }}
{% endblock %}
